- if object.errors.any?
  #error_explanation
    .alert.alert-error
      The form contains 
      = pluralize(object.errors.count, "error")
    %ul 
      - object.errors.each do |field, msg|
        %li= msg